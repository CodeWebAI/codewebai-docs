{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "pip install --break-system-packages -r requirements.txt && pnpm run build:css && python3 -m mkdocs build --strict",
    "outputDirectory": "site"
}